# Demo app (local run + Hugging Face Space). Superset of the extraction deps + gradio.
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pyyaml>=6.0
torch>=2.1
torchvision>=0.16
transformers>=4.40
pillow>=10.0
pillow-heif>=0.16        # iPhone .HEIC support
gradio>=4.0
