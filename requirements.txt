# Core (everything except the heavy extraction pass)
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3

# Extraction pass (run on the GPU box / Kaggle P100)
torch>=2.1
torchvision>=0.16
transformers>=4.40
pillow>=10.0

# Dev
pytest>=7.4
